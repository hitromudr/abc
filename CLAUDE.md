# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Что это

**abracadabra** — моно-репо из двух AI-агентов, работающих в паре:
- **abra** (abc) — Архитектор. Когнитивный конвейер для анализа сложных задач и генерации Концептуальных Протоколов.
- **cadabra** (xyz) — Исполнитель (Прораб). Получает машиночитаемый контракт (`EXECUTION_STATE.md`) от abra и автономно выполняет шаги.

Нет кода, сборки, тестов или линтера. Git используется только для версионирования документации. Язык — русский. Технические термины на языке оригинала.

## Структура

```
abracadabra/
├── abra/                       ← Архитектор (abc)
│   ├── core_rules.md           ← системный промпт abra (роль, правила, workflow)
│   ├── docs/
│   │   ├── 00_ИНИЦИАЛИЗАЦИЯ/   ← манифест, Двойная Спираль
│   │   ├── 01_БАЗА_ЗНАНИЙ/     ← 6 документов: фильтры, ландшафт, октагон, матрица, векторы, органика
│   │   ├── 02_ИНСТРУМЕНТЫ/     ← алгоритм разбора (UTO v5), шаблон протокола, автономный пайплайн, анализатор, оптимизатор, шаблон EXECUTION_STATE
│   │   └── 03_РЕШЕНИЯ/         ← готовые протоколы (мета, кросс-домены, аудиты)
│   └── scripts/
│       └── sync_context.sh     ← синхронизация контекста (v5.0)
├── cadabra/                    ← Исполнитель (xyz) — в разработке
│   ├── core_rules.md
│   ├── docs/
│   └── scripts/
├── .rules                      ← симлинк → abra/core_rules.md (для Zed)
├── .cursorrules                ← симлинк → abra/core_rules.md (для Cursor)
├── CLAUDE.md                   ← этот файл
└── README.md
```

## Заметки по совместимости

- `.rules` и `.cursorrules` — симлинки на `abra/core_rules.md`. Подхватываются Zed и Cursor соответственно.
- `abc` — текстовый ключ в промпте: AI распознаёт его из `.rules` и запускает pipeline. **ВАЖНО:** Последующие уточнения пишутся **БЕЗ** префикса `abc`.
- **ПРОБЛЕМА ХОЛОДНОГО СТАРТА:** Если пользователь пишет `abc [задача]`, но файлы базы знаний не прочитаны — агент **ОБЯЗАН сначала выполнить ПРОТОКОЛ ВОССТАНОВЛЕНИЯ КОНТЕКСТА**, и только потом решать задачу.
- Автономный пайплайн (`abra/docs/02_ИНСТРУМЕНТЫ/03_АВТОНОМНЫЙ_ПАЙПЛАЙН.md`) — сжатая версия для web-чатов.

## Правила из .rules (фактические)

1. **NO YAPPING** — запрещены преамбулы, извинения, клише. Только сигнал.
2. **THOUGHT PROCESS** — внутренний монолог в `<thought_process>` перед любым финальным ответом.
3. **THE DOUBLE HELIX** — каждая задача обязана двигать Product и Process одновременно.
4. **ПРОТОКОЛ ВОССТАНОВЛЕНИЯ КОНТЕКСТА** — при инициализации запускается `bash abra/scripts/sync_context.sh`, затем параллельное чтение всех файлов. Архитектурный лимит базы: ≤50% контекстного окна.
5. **EMPIRICAL GROUNDING** — без метрик/логов задача отклоняется.
6. **SEMANTIC GROUNDING** — абстракция обязана разворачиваться в физический факт.
7. **ГРАНИЦЫ ПРИМЕНИМОСТИ** — конвейер НЕ запускается на: Clear-домен, творческие задачи, задачи на скорость/интуицию.

## Формат протоколов

Выходной документ по шаблону `abra/docs/02_ИНСТРУМЕНТЫ/02_ШАБЛОН_ИТОГОВОГО_ПРОТОКОЛА.md`: Топология, Инварианты, Точка опоры, Векторы энтропии, Алгоритм стабилизации, Метрика истины, Эвристики, Резолюция (+ секция 8.1: генерация EXECUTION_STATE для cadabra), Верификация.

Контракт исполнителя: `abra/docs/02_ИНСТРУМЕНТЫ/06_ШАБЛОН_EXECUTION_STATE.md` — машиночитаемый артефакт для cadabra. Содержит: METADATA (стейт-машина), CONTEXT (scope), KILL BOX, DAG с верификацией, ERROR_LOG, COMPLETION_PROOF.
